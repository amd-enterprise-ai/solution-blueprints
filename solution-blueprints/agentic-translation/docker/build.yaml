# Copyright © Advanced Micro Devices, Inc., or its affiliates.
#
# SPDX-License-Identifier: MIT

services:
  application:
    build:
      context: ../
      dockerfile: docker/application.Dockerfile
    image: "${REGISTRY:?Must set REGISTRY}/${ORG:?Must set ORG}/image-aimsb-agentic-translation-application:${TAG:?Must set TAG}"
