{
  "name": "lemonade-telemetry-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Client-side inference-plane telemetry: a transparent reverse proxy in front of the local Lemonade server that emits an llm.request Splunk-HEC event per LLM call and runs DefenseClaw prompt/completion guardrails (observe, fail-open).",
  "bin": {
    "lemonade-telemetry-proxy": "src/server.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
