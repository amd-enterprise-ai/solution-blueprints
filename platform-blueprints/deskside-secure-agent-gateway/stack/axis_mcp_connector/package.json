{
  "name": "axis-mcp-connector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Client-side MCP connector: wraps every tool call in an AXIS sandbox (Landlock + seccomp + netns) and records a SQLite audit event.",
  "bin": {
    "axis-mcp-connector": "src/server.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "better-sqlite3": "^9.6.0",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=18"
  }
}
