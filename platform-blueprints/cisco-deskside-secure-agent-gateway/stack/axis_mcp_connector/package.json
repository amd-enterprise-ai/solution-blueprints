{
  "name": "axis-mcp-connector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Client-side MCP connector: wraps every tool call in an AXIS sandbox, runs DefenseClaw admission, and builds Splunk-HEC audit events.",
  "bin": {
    "axis-mcp-connector": "src/server.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=18"
  }
}
